# Web framework & ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ORM & migrations (analogous to EF Core + EF Core Tools)
sqlalchemy==2.0.51
alembic==1.14.0

# Settings management (analogous to appsettings.json / IConfiguration)
pydantic-settings==2.7.0

# Async database driver (swap sqlite for asyncpg/aiomysql for production)
# aiosqlite==0.20.0   # uncomment if using async SQLAlchemy with SQLite

# Production database drivers (uncomment as needed)
# psycopg2-binary==2.9.10   # PostgreSQL (sync)
# asyncpg==0.30.0            # PostgreSQL (async)
# pymysql==1.1.1             # MySQL (sync)

# Testing
pytest==8.3.5
httpx==0.28.1
